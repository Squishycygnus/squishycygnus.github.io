<footer>
    This blog is made with <a
        class="astro"
        href="https://astro.build"
        target="_blank">Astro</a
    > by <a class="noclaps" href="https://github.com/noClaps" target="_blank"
        >noClaps</a
    >
</footer>

<style lang="scss">
    footer {
        padding: 2rem;
        text-align: center;
    }

    a {
        text-decoration: none;
        position: relative;

        &::after {
            content: "";
            position: absolute;
            bottom: -0.25rem;
            left: 0;
            right: 0;
            height: 0.125rem;
            transform: scaleX(0);
            transition: all 0.25s;
            transform-origin: left;
        }

        &:hover::after {
            transform: scaleX(100%);
        }
    }

    .astro {
        color: var(--astro);

        &::after {
            background-color: var(--astro);
        }
    }

    .noclaps {
        color: var(--noclaps);

        &::after {
            background-color: var(--noclaps);
        }
    }
</style>
